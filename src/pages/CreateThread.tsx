import ThreadForm from "../components/ThreadForm";

const CreateThread = () => {
  return (
    <div>
      <h1>Skapa en ny tråd</h1>
      <ThreadForm />
    </div>
  );
};

export default CreateThread;
